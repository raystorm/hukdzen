# TestDesigner Rationale

## Return Type Specification

TestDesigner specifies return structure when scenarios depend on specific property paths.

**Why:** Ensures Builder knows exact property paths when structure matters for validation.
