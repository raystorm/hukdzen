# **Appendix A — Patch‑Package Details**

This appendix documents:

- the purpose of each patch
- the exact files patched inside `node_modules`
- the reason the patch exists
- the domains/features that depend on it
- any risks or maintenance notes

Patches are stored in:

```
/patches/
```

They are generated by **patch-package** and applied automatically on install.

---

# **A.1 Patch Workflow (Authoritative)**

### **Never edit patch files directly.**
Patch files are diffs — not source code.

> [!WARNING]
> Patch files will not Render correctly in IntellIJ. Ignore them.

### **Correct workflow:**

1. Modify the real file inside `node_modules`
2. Run patch-package to generate/update the patch
3. Run the `try-again` script to clear caches and reapply patches

### **Why `try-again` is required**

Node and Vite aggressively cache:

- resolved module paths
- transpiled JS
- sourcemaps
- type definitions
- ESM/CJS resolution results

Simply restarting the dev server is **not enough**.

Your `try-again` script performs the only reliable sequence:

- delete `node_modules`
- reinstall dependencies
- re-apply all patches
- clear caches
- ensure patched modules are actually used

This is the **only** correct way to refresh patched dependencies.

---

# **A.2 Patch: `@aws-amplify/storage`**

### **Purpose**

Fixes issues in Amplify Storage’s S3 provider that affect:

- file upload behavior
- metadata handling
- error propagation
- type definitions
- sourcemap correctness

These fixes are required for:

- the FileUploader domain
- ingestion alignment
- consistent behavior between local and cloud uploads

### **Patched Files**

These paths come directly from the patch diff headers
and represent the *exact* files modified by the patch.


#### **CJS Build**

```
node_modules/@aws-amplify/storage/dist/cjs/types/common.js.map
```

#### **ESM Build**

```
node_modules/@aws-amplify/storage/dist/esm/providers/s3/apis/internal/uploadData/index.mjs
node_modules/@aws-amplify/storage/dist/esm/providers/s3/utils/transferTask.mjs
node_modules/@aws-amplify/storage/dist/esm/types/common.d.ts
node_modules/@aws-amplify/storage/dist/esm/types/common.mjs.map
```

#### **TypeScript Source (Upstream)**

```
node_modules/@aws-amplify/storage/src/providers/s3/utils/transferTask.ts
node_modules/@aws-amplify/storage/src/types/common.ts
```

These files collectively define:

- the upload state machine
- the TransferTask interface
- the ESM and CJS builds consumed by Vite, Jest, and the browser
- sourcemaps used for debugging

### **Why these files**

#### **1. Upload Behavior (ESM)**
`uploadData/index.mjs` and `transferTask.mjs` contain the runtime logic for:

- single-part vs multipart uploads
- cancellation
- pause/resume
- state transitions
- error handling
- logging

The patches modify these behaviors directly.

#### **2. TransferTask State Mutability (TS + ESM)**
Changed:

```
readonly state → state
```

This affects:

- the TypeScript source (`src/types/common.ts`)
- the generated ESM types (`dist/esm/types/common.d.ts`)
- sourcemaps (`common.js.map`, `common.mjs.map`)

#### **3. Source of Truth (TS)**
`src/providers/s3/utils/transferTask.ts` is the upstream TypeScript implementation.  
Your patch corrects:

- state handling
- cancellation semantics
- error propagation
- final state assignment

---

#### **Domains depending on this patch**

- **FileUploader** (primary)
- **docs** (indirectly, via ingestion)
- **Translator** (indirectly, via upload flows)

### **Risks / Maintenance Notes**

- Amplify updates may break or overwrite the patch
- After upgrading Amplify, regenerate the patch
- Always run `try-again` after modifying or regenerating the patch

---

# **A.3 Patch: (Reserved for future patches)**

This section is intentionally left open for:

- UI library patches
- React component library patches
- AWS SDK patches
- Vite plugin patches

Each new patch should include:

- **Purpose**
- **Patched files**
- **Why those files**
- **Domains depending on it**
- **Risks / maintenance notes**

---

# **A.4 Patch Naming Convention**

Patch files follow the format:

```
<package-name>+<version>.patch
```

Example:

```
@aws-amplify+storage+6.4.1.patch
```

This ensures:

- patches are tied to specific versions
- accidental upgrades break loudly
- contributors can see exactly what changed

---

# **A.5 Contributor Notes**

### **Do not create patches for:**

- app code
- domain code
- components
- utils
- anything inside `src/`

### **Do create patches for:**

- upstream bugs
- missing type definitions
- incorrect sourcemaps
- incorrect ESM/CJS exports
- behavior mismatches between local and cloud

### **Always document the reason for the patch.**
Future contributors must understand why it exists.

---

# **A.6 Quick Reference**

| Task | Command |
|------|---------|
| Modify dependency | Edit file in `node_modules` |
| Generate patch | `npx patch-package <package>` |
| Apply patches | Happens automatically on install |
| Clear caches + reapply | `npm run try-again` |
| View patches | `patches/` folder |
