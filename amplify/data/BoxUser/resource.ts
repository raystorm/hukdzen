// import { boxUserHydrator } from '../../functions/data/BoxUserHydrator/infra/backend';

export function wireBoxUserResolvers(data: any)
{
   //const hydrator = data.addLambdaDataSource('BoxUserHydratorDS', boxUserHydrator);

   //data.addResolver('Query', 'listBoxUsersDetailed', { dataSource: hydrator, });
}
