{
  "name": "amplify-tests",
  "version": "1.0.0",
  "description": "Vitest environment for Amplify guard resolvers",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "@vitest/coverage-v8": "^1.2.2",
    "vitest": "^1.2.2"
  }
}
